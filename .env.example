# Database Configuration
DB_NAME=postdb
DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_PORT=5432

# Backend Configuration
# Backend instances run on port 9090 internally
# Exposed ports for direct access: 9091, 9092, 9093
BACKEND_PORT=9090

# NGINX Load Balancer (recommended)
# Access the API via: http://localhost
NGINX_PORT=80

# Note: Copy this file to .env and modify as needed
# Command: cp .env.example .env
