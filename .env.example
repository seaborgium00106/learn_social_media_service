# Database Configuration
DB_NAME=postdb
DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_PORT=5432

# Backend Configuration
# Backend instances run on port 9090 internally
# Exposed ports for direct access: 9091, 9092, 9093
BACKEND_PORT=9090

# NGINX Load Balancer (recommended)
# Access the API via: http://localhost
NGINX_PORT=80

# Frontend Configuration
# Frontend port (dev server)
FRONTEND_PORT=3000

# Frontend API Configuration
# Base URL for API calls from the frontend
# In Docker, use 'http://localhost' to go through NGINX load balancer
# Or use 'http://nginx' to use the service name (for internal Docker network)
VITE_API_BASE_URL=http://localhost
VITE_API_TIMEOUT=10000
VITE_APP_NAME=Social Network
VITE_DEFAULT_PAGE_SIZE=10

# Frontend HMR Configuration (for hot module replacement in development)
# These are used when running in Docker development mode
VITE_HMR_HOST=localhost
VITE_HMR_PORT=3000
VITE_HMR_PROTOCOL=http

# Note: Copy this file to .env and modify as needed
# Command: cp .env.example .env

# DOCKER MODE:
# Development: docker-compose up
# Production: docker-compose -f docker-compose.yml build --build-arg TARGET=production && docker-compose up
