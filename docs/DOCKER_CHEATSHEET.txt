═══════════════════════════════════════════════════════════════════
    🐳 DOCKER CHEAT SHEET - Essential Commands
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ FIRST TIME SETUP (5 minutes)                                    │
└─────────────────────────────────────────────────────────────────┘

1. Build:       docker-compose build
2. Start:       docker-compose up -d
3. Verify:      docker-compose ps
4. Test:        curl http://localhost:9090/api/v1/users


┌─────────────────────────────────────────────────────────────────┐
│ EVERYDAY USE                                                     │
└─────────────────────────────────────────────────────────────────┘

Start:          docker-compose up -d
Stop:           docker-compose stop
Restart:        docker-compose restart backend
Status:         docker-compose ps
Logs:           docker-compose logs backend
Live Logs:      docker-compose logs -f backend


┌─────────────────────────────────────────────────────────────────┐
│ AFTER CODE CHANGES                                               │
└─────────────────────────────────────────────────────────────────┘

Quick:          docker-compose up -d --build

Or step by step:
                docker-compose build
                docker-compose down
                docker-compose up -d


┌─────────────────────────────────────────────────────────────────┐
│ DATABASE ACCESS                                                  │
└─────────────────────────────────────────────────────────────────┘

Connect:        docker exec -it postdb_postgres psql -U postgres -d postdb

Once inside:    \dt                    (list tables)
                \d users               (table structure)
                SELECT * FROM users;   (view data)
                \q                     (exit)

Quick Query:    docker exec postdb_postgres psql -U postgres -d postdb -c "SELECT * FROM users;"


┌─────────────────────────────────────────────────────────────────┐
│ API TESTING                                                      │
└─────────────────────────────────────────────────────────────────┘

List Users:     curl http://localhost:9090/api/v1/users

Create User:    curl -X POST http://localhost:9090/api/v1/users \
                  -H "Content-Type: application/json" \
                  -d '{"username":"test","email":"test@example.com"}'

List Posts:     curl http://localhost:9090/api/v1/posts

Timeline:       curl http://localhost:9090/api/v1/timeline/1


┌─────────────────────────────────────────────────────────────────┐
│ CLEANUP                                                          │
└─────────────────────────────────────────────────────────────────┘

Stop (keep data):       docker-compose down
Delete ALL data:        docker-compose down -v
Nuclear option:         docker-compose down -v && docker system prune -a


┌─────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                  │
└─────────────────────────────────────────────────────────────────┘

View Logs:      docker-compose logs backend
Check Health:   docker-compose ps
Restart:        docker-compose restart backend
Fresh Start:    docker-compose down -v && docker-compose up -d


┌─────────────────────────────────────────────────────────────────┐
│ CURRENT SETUP                                                    │
└─────────────────────────────────────────────────────────────────┘

Backend URL:    http://localhost:9090
Database:       localhost:5432
Containers:     postdb_backend, postdb_postgres
API Base:       /api/v1/


┌─────────────────────────────────────────────────────────────────┐
│ FILES                                                            │
└─────────────────────────────────────────────────────────────────┘

docker-compose.yml          - Container orchestration
.env                        - Environment variables
backend/Dockerfile          - Backend image
DOCKER_LOCAL_SETUP.md       - Full documentation
DOCKER_COMMANDS.sh          - Helper script
DOCKER_CHEATSHEET.txt       - This file


═══════════════════════════════════════════════════════════════════

Pro Tips:
  • Use -d flag to run in background (detached mode)
  • Use -f to follow logs in real-time
  • Data persists in postgres_data volume
  • Network is backend-network

═══════════════════════════════════════════════════════════════════
